#include<conio.h>
#include<stdio.h>
#include<alloc.h>
#include<stdlib.h>
tpedef struct treenode t1
{
	int data;
	struct treenode *lchild,*rchild;
	}treenode;
void create(treenode **t1);
void display(treenode *t1);
void main()
{
	printf("enter the nos.in binary search tree");
	create(&t1);
	display(t1);
	printf("do u wwish to insert any val y/n");
	scanf("%c",&x);
	if(x=='y')
	insert(t1);
	getch();
	}
void create(treenode **t1)
{
	int val;
   tr
	printf("enter value is binary search tree (-1 to exit):");
	scanf("%d",val);
	while(val!=-1)
	{
		if(*t1==NULL)
		{

			*t1=(treenode*)malloc(sizeof(treenode));
			*t1->data=val;
			temp->lchild=NULL;
			temp->rchild=NULL;
		 }
		else




